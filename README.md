# mlp-cifar2-classification
MLP model implementation in Python using NumPy for classifying the CIFAR-10 dataset into two classes. Includes forward and backward passes, training, and evaluation loops.
